{
  "name": "knowledge-graph-web",
  "version": "0.0.1",
  "description": "",
  "keywords": [],
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build --no-optimize --dist-dir dist/dev index.html tmpl/*.html",
    "ci-build": "node ci-build.js",
    "dev": "parcel index.html tmpl/*.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@parcel/core": "^2.2.0",
    "@parcel/packager-raw-url": "^2.0.0",
    "@parcel/transformer-webmanifest": "^2.0.0",
    "eslint-plugin-promise": "^5.1.1",
    "fs-extra": "^10.0.0"
  },
  "dependencies": {
    "@picocss/pico": "^1.4.1",
    "gridjs": "^5.0.2",
    "js-cookie": "^3.0.1",
    "mustache": "^4.2.0",
    "parcel": "^2.0.0"
  }
}
